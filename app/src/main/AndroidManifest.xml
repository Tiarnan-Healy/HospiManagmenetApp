<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hospimanagmenetapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Lab 2: Biometric -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Lab3: QR -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="HospiManagmenetApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.PatientRegistrationActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".ui.AdminLoginActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".ui.AdminPortalActivity"
            android:exported="false"
            android:parentActivityName=".ui.AdminLoginActivity" />

        <!-- Lab 2 -->
        <activity
            android:name=".feature.appointments.ui.AppointmentActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Lab 3: EHR screens -->
        <activity
            android:name=".feature.ehr.ui.PatientSummaryActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".feature.ehr.ui.VitalsActivity"
            android:exported="false"
            android:parentActivityName=".feature.ehr.ui.PatientSummaryActivity" />

        <activity
            android:name=".feature.ehr.ui.EditClinicalRecordActivity"
            android:exported="false"
            android:parentActivityName=".feature.ehr.ui.PatientSummaryActivity" />

        <activity
            android:name=".feature.ehr.ui.BarcodeScannerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
    </application>
</manifest>